# reflaxe.ruby std/_std

This directory is for upstream Haxe std overrides that must precede normal std classpaths.

Do not add files here for convenience. Each override should have an entry in `docs/stdlib-inventory.json` explaining the owner, status, and reason.
