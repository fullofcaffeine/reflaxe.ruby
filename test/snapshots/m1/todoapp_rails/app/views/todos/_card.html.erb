<section class="<%= ("card " + "typed-dashboard") %>">
			<div class="typed-dashboard-header">
				<span class="eyebrow"><%= eyebrow %></span>
				<h2><%= title %></h2>
			</div>
			<%= body %>
		</section>
