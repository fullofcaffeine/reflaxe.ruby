<% todos ||= [] %>
<% todo_count = todos.length %>
<% sample_user = Models::User.order(:id).first %>

<style>
  :root {
    --ink: #18221f;
    --muted: #68736d;
    --paper: #fffaf0;
    --card: rgba(255, 255, 255, 0.76);
    --mint: #b9f4d3;
    --ember: #ff6b35;
    --sun: #ffd166;
    --line: rgba(24, 34, 31, 0.14);
  }

  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    min-height: 100vh;
    color: var(--ink);
    font-family: "Iowan Old Style", "Palatino Linotype", Georgia, serif;
    background:
      radial-gradient(circle at 16% 16%, rgba(255, 209, 102, 0.72), transparent 26rem),
      radial-gradient(circle at 88% 10%, rgba(185, 244, 211, 0.82), transparent 24rem),
      linear-gradient(135deg, #fff6df 0%, #eef8ef 52%, #f7e7d8 100%);
  }

  .todo-shell {
    width: min(1120px, calc(100% - 32px));
    margin: 0 auto;
    padding: 56px 0;
  }

  .hero {
    display: grid;
    grid-template-columns: 1.08fr 0.92fr;
    gap: 28px;
    align-items: stretch;
  }

  .hero-card,
  .panel {
    border: 1px solid var(--line);
    border-radius: 34px;
    background: var(--card);
    box-shadow: 0 24px 80px rgba(24, 34, 31, 0.16);
    backdrop-filter: blur(20px);
  }

  .hero-card {
    padding: 42px;
    position: relative;
    overflow: hidden;
  }

  .hero-card::after {
    content: "";
    position: absolute;
    right: -70px;
    bottom: -82px;
    width: 220px;
    height: 220px;
    border-radius: 999px;
    background: linear-gradient(135deg, var(--ember), var(--sun));
    opacity: 0.24;
  }

  .eyebrow {
    display: inline-flex;
    gap: 8px;
    align-items: center;
    margin-bottom: 26px;
    padding: 8px 12px;
    border-radius: 999px;
    background: rgba(24, 34, 31, 0.08);
    color: #365249;
    font: 700 0.76rem/1.1 "Avenir Next", "Trebuchet MS", sans-serif;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  h1 {
    max-width: 760px;
    margin: 0;
    font-size: clamp(3.2rem, 8vw, 7.6rem);
    line-height: 0.88;
    letter-spacing: -0.075em;
  }

  .hero-copy {
    max-width: 620px;
    margin: 24px 0 0;
    color: var(--muted);
    font: 1.16rem/1.7 "Avenir Next", "Trebuchet MS", sans-serif;
  }

  .stats {
    display: grid;
    gap: 14px;
    padding: 24px;
  }

  .stat {
    padding: 22px;
    border-radius: 24px;
    background: rgba(255, 250, 240, 0.72);
    border: 1px solid var(--line);
  }

  .stat strong {
    display: block;
    font-size: 3rem;
    letter-spacing: -0.06em;
  }

  .stat span {
    color: var(--muted);
    font: 700 0.78rem/1 "Avenir Next", "Trebuchet MS", sans-serif;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .workspace {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    gap: 28px;
    margin-top: 28px;
  }

  .panel {
    padding: 28px;
  }

  .panel h2 {
    margin: 0 0 18px;
    font-size: 2rem;
    letter-spacing: -0.04em;
  }

  .todo-form {
    display: grid;
    gap: 14px;
  }

  .todo-form label {
    color: var(--muted);
    font: 800 0.78rem/1 "Avenir Next", "Trebuchet MS", sans-serif;
    letter-spacing: 0.11em;
    text-transform: uppercase;
  }

  .todo-form input {
    width: 100%;
    border: 1px solid var(--line);
    border-radius: 18px;
    padding: 16px 18px;
    color: var(--ink);
    background: rgba(255, 255, 255, 0.78);
    font: 1rem/1.2 "Avenir Next", "Trebuchet MS", sans-serif;
    outline: none;
  }

  .todo-form input:focus {
    border-color: rgba(255, 107, 53, 0.62);
    box-shadow: 0 0 0 5px rgba(255, 107, 53, 0.12);
  }

  .todo-form button {
    border: 0;
    border-radius: 18px;
    padding: 16px 18px;
    color: white;
    cursor: pointer;
    font: 900 0.9rem/1 "Avenir Next", "Trebuchet MS", sans-serif;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    background: linear-gradient(135deg, #18221f, #365249);
    box-shadow: 0 16px 38px rgba(24, 34, 31, 0.24);
  }

  .todo-list {
    display: grid;
    gap: 12px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .todo-item {
    display: flex;
    gap: 14px;
    align-items: center;
    padding: 18px;
    border-radius: 22px;
    background: rgba(255, 255, 255, 0.68);
    border: 1px solid var(--line);
  }

  .todo-dot {
    flex: 0 0 auto;
    width: 14px;
    height: 14px;
    border-radius: 999px;
    background: var(--ember);
    box-shadow: 0 0 0 6px rgba(255, 107, 53, 0.14);
  }

  .todo-title {
    font: 800 1rem/1.35 "Avenir Next", "Trebuchet MS", sans-serif;
  }

  .empty-state {
    padding: 28px;
    border: 1px dashed rgba(24, 34, 31, 0.24);
    border-radius: 24px;
    color: var(--muted);
    background: rgba(255, 255, 255, 0.48);
    font: 1rem/1.6 "Avenir Next", "Trebuchet MS", sans-serif;
  }

  @media (max-width: 820px) {
    .hero,
    .workspace {
      grid-template-columns: 1fr;
    }

    .todo-shell {
      padding: 28px 0;
    }

    .hero-card,
    .panel {
      border-radius: 26px;
      padding: 24px;
    }
  }
</style>

<main class="todo-shell">
  <section class="hero">
    <div class="hero-card">
      <span class="eyebrow">RailsHx sample</span>
      <h1>Typed Rails, polished Ruby.</h1>
      <p class="hero-copy">
        This todo app is authored in Haxe with typed ActiveRecord metadata,
        strong params, route helpers, and Rails-native rendering.
      </p>
    </div>

    <aside class="stats panel" aria-label="Todo stats">
      <div class="stat">
        <strong><%= todo_count %></strong>
        <span>open tasks</span>
      </div>
      <div class="stat">
        <strong><%= Models::Todo.__hx_rails_schema[:columns].length %></strong>
        <span>typed columns</span>
      </div>
    </aside>
  </section>

  <section class="workspace">
    <div class="panel">
      <h2>Add a task</h2>
      <% if sample_user %>
        <%= form_with url: todos_path, scope: :todo, local: true, class: "todo-form" do |form| %>
          <%= form.hidden_field :user_id, value: sample_user.id %>
          <%= form.hidden_field :is_completed, value: false %>
          <div>
            <%= form.label :title, "What should ship next?" %>
            <%= form.text_field :title, placeholder: "Write the migration DSL", required: true %>
          </div>
          <%= form.submit "Add task", type: "submit" %>
        <% end %>
      <% else %>
        <div class="empty-state">
          Create a user first; the integration fixture seeds one before exercising this page.
        </div>
      <% end %>
    </div>

    <div class="panel">
      <h2>Open work</h2>
      <% if todos.any? %>
        <ul class="todo-list">
          <% todos.each do |todo| %>
            <li class="todo-item">
              <span class="todo-dot" aria-hidden="true"></span>
              <span class="todo-title"><%= todo.title %></span>
            </li>
          <% end %>
        </ul>
      <% else %>
        <div class="empty-state">
          No open tasks. Suspiciously serene.
        </div>
      <% end %>
    </div>
  </section>
</main>
