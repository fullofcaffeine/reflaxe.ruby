package;

/**
	Shared target token used by both the Hotwire contract and its owning HHX
	view, making target renames compiler-checked on both sides.
**/
class RoomHooks {
	public static inline final rowsId = "room-rows";
}
