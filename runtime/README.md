# reflaxe.ruby runtime

Stable Ruby runtime files should live under `runtime/hxruby/`.

The compiler currently emits small compatibility snippets directly for early fixtures. As M3 proceeds, those snippets should move here and be copied or required by generated output.

See `docs/stdlib-ownership.md` and `docs/stdlib-inventory.json`.
