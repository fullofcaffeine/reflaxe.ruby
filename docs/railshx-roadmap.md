# RailsHx Roadmap

RailsHx is the Rails-first layer for `reflaxe.ruby`: typed Haxe authoring that emits idiomatic Rails-shaped Ruby. It should grow from the current Rails MVP into a first-class framework integration comparable in architecture to `../haxe.elixir.codex`'s Phoenix/Ecto implementation.

## Current State

The repo currently has a Rails MVP:

- Rails output roots and autoload affordances via `-D reflaxe_ruby_rails`.
- Minimal ActiveRecord model surface through `rails.active_record.Base<T>` and `rails.macros.ModelMacro`, including generated model-owned field refs such as `Todo.fields.title` / `Todo.f.title : Field<Todo, String>`, generated association refs such as `Todo.associations.user` / `Todo.a.user : Association<Todo, User>`, and params/form scopes such as `Todo.railsParamKey : ModelKey<Todo>`.
- Initial typed ActiveRecord relation chain through `Relation<T, TCriteria>`, typed model/relation `where({...})` and `rewhere({...})` criteria, typed predicate builders such as `whereNot({...})`, `whereIn(Todo.f.status, [...])`, `whereBetween(Todo.f.id, 1, 10)`, Arel-backed comparisons such as `whereGt(Todo.f.id, 1)` / `whereNotLte(Todo.f.id, 10)`, fluent expression predicates such as `where(Todo.f.title.lower().eq("ship"))`, and nullable-only `whereNull(Todo.f.notes)`, typed relation `or(...)`/`merge(...)`, typed association `includes(...)`/`joins(...)`, typed `find(...)`/`findBy({...})`/`exists({...})`, `all()`, `distinct()`, `none()`, `reverseOrder()`, `readOnly()`, `select(Todo.f.title)`, `order(Todo.f.title.asc())`, `order(Todo.f.title.lower().asc())`, `reorder(Todo.f.id.desc())`, typed multi-field `Order.many([...])`, `limit(...)`, `offset(...)`, typed single-field `pluck(Todo.f.title)`, named multi-field `Projection.pluck(...)`, grouped aggregate `Projection.group(...)`, typed `Group.count(...)` for `StringMap<Int>`/`IntMap<Int>` grouped counts, typed `Group.countHaving(...)` with aggregate predicates such as `Todo.f.id.count().gt(1)`, typed `minimum(Todo.f.id)` / `maximum(Todo.f.id)`, typed `sum(Todo.f.userId)` / `average(Todo.f.userId)` for `Int` field refs, typed Rails scope declarations through `@:railsScope` / `@:railsDefaultScope`, `count()`, `first()`, `last()`, and `toArray()`, while emitting normal Rails `ActiveRecord::Relation` and model scope calls.
- Minimal ActionController/strong params surface through `rails.action_controller.Base`, `Params`, and `ParamsMacro`, including typed model-scope validation for `ParamsMacro.requirePermit(this.params(), Todo.railsParamKey, [Todo.f.title])`, typed request/response facades, method-backed ActionController filters, typed flash/session/cookies stores, and typed status tokens for helpers such as `head(Status.noContent)`.
- First typed ActionView render seam through `rails.action_view.Template<TLocals>`, `ViewMacro.renderTemplate(...)`, and `@:railsTemplate(...)` artifact generation.
- First typed ActionView AST seam through Rails HHX inline markup, `rails.action_view.H`, `HtmlNode`, `HtmlAttr`, and `@:railsTemplateAst(...)`, currently proven by the todoapp's generated index shell, generated partials, typed partial composition, typed route-helper links, and initial typed form helpers.
- First gradual-adoption seam through `Template.existing("...") : Template<TLocals>`/`Template.external("...") : Template<TLocals>` plus typed Ruby externs, proven by `examples/rails_interop_app`: Haxe renders an existing ERB partial with typed locals, Haxe calls a legacy Ruby service, legacy ERB renders generated HHX output, and legacy Ruby/ERB calls a generated Haxe service as a normal Ruby constant.
- First typed migration artifact seam through Haxe-authored `@:railsMigration(...)` classes, which emit standard timestamped `db/migrate/*.rb` ActiveRecord migrations. The compatibility path can derive create-table output from referenced ActiveRecord model metadata; the production-preferred path uses explicit operation snapshots such as `CreateTable` with typed id/id-type/primary-key/comment/temporary options, `ChangeTable` with typed column/default/null change, column/index rename, column/reference/index add/remove bulk blocks, table-block foreign-key, unique-constraint, and exclusion-constraint add/remove including guarded constraint forms, check-constraint add/remove, and timestamp add/remove, `Column`, `Reference` with typed id-type/comment/index columns, typed foreign-key hashes including deferred validation, and polymorphic foreign-key diagnostics, `Index`, typed column comments, typed index comments, using methods, index types, guarded concurrent index algorithms through `disableDdlTransaction`, index lock modes, prefix lengths, opclasses, ordered columns, include columns, and nulls-not-distinct options, reversible comment changes, typed timestamp add/remove, typed join table create/drop with typed column options, checked extension enable/disable, checked schema create/drop/rename, checked enum create/drop/add/type/value rename, checked column bounds, idempotent create/drop tables, idempotent add/remove columns/references, named/idempotent/deferrable foreign keys and named/idempotent foreign-key removal, named/idempotent indexes, named/idempotent index removal, reversible index renames, checked index enable/disable, typed default/null backfill changes, deferred FK/check validation including table-block validation commands, `AddReference`, `AddCompositeIndex`, `RemoveCompositeIndex`, safe named/idempotent check constraints, generic named constraint validation/removal, checked unique constraints including idempotent creation/removal and using-index conversion, checked exclusion constraints including idempotent creation/removal, renames, change-null, reversible destructive operations, and explicit SQL/data migrations with rollback.
- Ruby-native Rails-facing generators under `lib/hxruby/generators`, Rails generator adapters under `lib/generators/hxruby`, and thin scripts under `scripts/rails`.
- A Rails todoapp example and optional Rails integration smoke coverage.

That MVP is useful, but it is not yet a RailsHx compiler layer. RailsHx should add typed framework APIs, macro metadata registries, generated Rails artifacts, app generators, and end-to-end Rails tests.

## Reference Implementation Pattern

Use `../haxe.elixir.codex` as the local inspiration, especially:

- Ecto schema registration and association validation for ActiveRecord model metadata.
- Ecto typed query APIs for typed `ActiveRecord::Relation` authoring.
- Ecto migration DSL, builder, registry, and validator for Rails migration generation.
- Phoenix typed controller/assign/params boundaries for Rails controller and strong-params boundaries.
- Phoenix router DSL and generated helpers for Rails route helper sync, adapted to Rails-owned and Haxe-owned route source-of-truth modes.
- Mix/project generator docs and end-to-end Phoenix/Ecto examples for RailsHx generators and integration tests.

The goal is not to make Rails look like Phoenix. The goal is to reuse the same successful compiler architecture: typed std surfaces plus macros/registries/tooling that lower into normal framework code.

For generators, that means adapting the PhoenixHx Mix-task split to Rails: in-place app tooling is Ruby/Rails-native and packages with the gem; optional Haxe-authored generator dogfooding can come later after the public Ruby contract is stable.

Official Rails behavior remains the output contract. RailsHx should follow Rails' Active Record query, association, validation, migration, routing, controller, generator, and testing conventions rather than inventing a parallel framework.

Rails runtime tasks remain Rails-owned. RailsHx generators should create or adopt
typed Haxe source and checked contracts, then the compiler emits normal
Rails-shaped artifacts. The public workflow is `bin/rails generate hxruby:*`,
RailsHx compile-then-delegate tasks such as `bundle exec rake hxruby:db:migrate`
and `bundle exec rake hxruby:test`, then Rails-owned checks such as
`bin/rails zeitwerk:check` and
`assets:precompile`. See [RailsHx Generators And Rails Tasks Design](railshx-generators-and-tasks-design.md).

## Typed DSL Rule

RailsHx should use macros and typed std stubs when Rails APIs need a DSL. The `../haxe.elixir.codex` HXX/HEEx layer is the model: Haxe source should type-check template/DSL expressions, the compiler should intercept or rewrite those typed forms at compile time, and generated output should be plain framework-native artifacts. For Rails this means typed model, query, migration, params, route-helper, and template DSLs should lower into normal Rails Ruby, ERB/Action View artifacts, migrations, routes, and tests.

Do not build a parallel runtime DSL engine by default. Compile-time-only helpers are preferred when they can disappear after lowering. Runtime helpers are acceptable only when Rails itself needs a callable boundary or when tests prove that a small helper gives a clearer Rails-native contract.

For typed ActionView templates, follow the HXX lesson closely: provide typed entrypoints for locals/assigns/components, parse embedded Haxe expressions through macro typing, validate field/helper references before Ruby runs, and emit Rails-native renderable templates/components. `@:railsTemplateAst("render")` is the first implementation slice: a static Haxe method returns a typed `HtmlNode` tree. The preferred app-facing authoring style is Rails HHX inline markup, for example `return <div>${todo.title}</div>`, with `reflaxe.ruby.macros.RailsInlineMarkup` rewriting Haxe's parser-produced `@:markup` payload into `HtmlNode`/`HtmlAttr` constructors. Embedded expressions such as `todo.title` are type-checked by Haxe; an argument named `locals` acts as a typed Rails locals object and `locals.sampleUserId` lowers to the partial local `sample_user_id`; `<if ${condition}>` lowers to typed template branches; `<for ${todo in todos}>` lowers to typed template loops; `<link_to>`, `<button_to>`, `<button_tag>`, `<submit_tag>`, `<text_field_tag>`, `<search_field_tag>`, `<email_field_tag>`, `<telephone_field_tag>`, `<url_field_tag>`, `<number_field_tag>`, `<range_field_tag>`, `<color_field_tag>`, `<date_field_tag>`, `<time_field_tag>`, `<datetime_field_tag>`, `<month_field_tag>`, `<week_field_tag>`, `<password_field_tag>`, `<hidden_field_tag>`, `<file_field_tag>`, `<text_area_tag>`, `<check_box_tag>`, `<radio_button_tag>`, `<image_tag>`, `<picture_tag>`, `<favicon_link_tag>`, `<preload_link_tag>`, `<javascript_include_tag>`, `<javascript_tag>`, `<auto_discovery_link_tag>`, `<audio_tag>`, `<video_tag>`, `<mail_to>`, `<phone_to>`, `<sms_to>`, `<pluralize>`, `<simple_format>`, `<truncate>`, `<excerpt>`, `<highlight>`, `<word_wrap>`, `<sanitize>`, `<sanitize_css>`, `<strip_tags>`, `<strip_links>`, `<to_sentence>`, `<escape_once>`, `<cdata_section>`, `<safe_join>`, `<token_list>`, `<class_names>`, `<cycle>`, `<current_cycle>`, `<reset_cycle>`, `<time_ago_in_words>`, `<distance_of_time_in_words>`, `<time_tag>`, `<number_to_currency>`, `<number_to_percentage>`, `<number_to_human>`, `<number_to_human_size>`, `<number_with_precision>`, `<number_with_delimiter>`, `<number_to_delimited>`, `<number_to_phone>`, and `<partial>` lower to Rails helpers while preserving route, button content, submit value, standalone field name/value, search field name/value, email field name/value, telephone field name/value, URL field name/value, number field name/numeric value, range field name/numeric value, color field name/value, date field name/value, time field name/value, datetime field name/value, month field name/value, week field name/value, password field name/value, hidden field name/value, file field name, text area name/content, check box name/value/checked, radio button name/value/checked, asset source, preload option, script content, feed option, media option, attribute, phone, sms, count, text, html, allowed sanitize tags/attributes, CSS style, collection item, token, class token, cycle value/name, time, numeric value, formatting option, and locals type checks; `<partial locals=${{sampleUserId: value}}>` keys lower to Rails locals such as `sample_user_id` so composition matches generated partial bodies; helper tags can use `text="..."`, static text, or `${...}` expression children for labels; `<link_to>`, `<button_to>`, `<button_tag>`, `<submit_tag>`, `<mail_to>`, `<phone_to>`, and `<sms_to>` support text labels, and `<link_to>`/`<button_to>` can also wrap nested HHX and lower to Rails block-form helpers (`link_to ... do` / `button_to ... do`); typed tags such as `<form_with scope=${Todo.railsParamKey}>`, `<hidden_field name=${Todo.f.userId}>`, `<field_label name=${Todo.f.title}>`, `<text_field name=${Todo.f.title}>`, `<search_field name=${Todo.f.title}>`, `<select name=${User.f.role} options=${[{label: "Member", value: "member"}]}>`, `<field_errors name=${Todo.f.title}>`, `<text_area name=${Todo.f.notes}>`, `<check_box>`, and `<submit>` lower to Rails form builders; and the compiler emits normal `.html.erb`. RailsHx-owned template references should prefer `Template.of(ViewClass)` and `Template.layout(LayoutViewClass)`, leaving `Template.named("path")` as a lower-level escape for rare cases. For existing Rails/Poc ERB, use `Template.existing("path") : Template<TLocals>` instead of pretending RailsHx owns the file; the compiler should type-check the Haxe locals object, check the ERB file where possible, and emit a normal Rails `render partial:` call without emitting or overwriting that external template. The macro-only `rails.action_view.H` helper facade remains a lower-level IR/construction surface for explicit implementation slices, tests, and escape hatches; when explicit construction needs cross-file hooks, accessibility attrs, or Rails helper calls, use checked helpers such as `H.data("turbo-frame", value)`, `H.dataBool("railshx-scroll")`, `H.aria("label", value)`, `H.role("status")`, `H.buttonTag(content, attrs)`, `H.submitTag(value, attrs)`, `H.textFieldTag(name, value, attrs)`, `H.searchFieldTag(name, value, attrs)`, `H.emailFieldTag(name, value, attrs)`, `H.telephoneFieldTag(name, value, attrs)`, `H.urlFieldTag(name, value, attrs)`, `H.numberFieldTag(name, value, attrs)`, `H.rangeFieldTag(name, value, attrs)`, `H.colorFieldTag(name, value, attrs)`, `H.dateFieldTag(name, value, attrs)`, `H.timeFieldTag(name, value, attrs)`, `H.datetimeFieldTag(name, value, attrs)`, `H.monthFieldTag(name, value, attrs)`, `H.weekFieldTag(name, value, attrs)`, `H.passwordFieldTag(name, value, attrs)`, `H.hiddenFieldTag(name, value, attrs)`, `H.fileFieldTag(name, attrs)`, `H.textAreaTag(name, content, attrs)`, `H.checkBoxTag(name, value, checked, attrs)`, `H.radioButtonTag(name, value, checked, attrs)`, `H.imageTag(source, attrs)`, `H.pictureTag(source, attrs)`, `H.faviconLinkTag(source, attrs)`, `H.preloadLinkTag(source, attrs)`, `H.javascriptIncludeTag(source, attrs)`, `H.javascriptTag(content, attrs)`, `H.autoDiscoveryLinkTag(feedType, url, attrs)`, `H.audioTag(source, attrs)`, `H.videoTag(source, attrs)`, `H.mailTo(email, label, attrs)`, `H.phoneTo(phone, label, attrs)`, `H.smsTo(phone, label, attrs)`, `H.pluralize(count, singular, plural)`, `H.simpleFormat(text, attrs)`, `H.truncate(text, length, omission)`, `H.excerpt(text, phrase, radius, omission)`, `H.highlight(text, phrase, highlighter, sanitize)`, `H.wordWrap(text, lineWidth, breakSequence)`, `H.sanitize(html, tags, attributes)`, `H.sanitizeCss(style)`, `H.stripTags(html)`, `H.stripLinks(html)`, `H.toSentence(items, wordsConnector, twoWordsConnector, lastWordConnector)`, `H.escapeOnce(html)`, `H.cdataSection(content)`, `H.safeJoin(items, separator)`, `H.tokenList(tokens)`, `H.classNames(tokens)`, `H.cycle(values, name)`, `H.currentCycle(name)`, `H.resetCycle(name)`, `H.timeAgoInWords(fromTime, includeSeconds)`, `H.distanceOfTimeInWords(fromTime, toTime, includeSeconds)`, `H.timeTag(time, label, attrs)`, `H.numberToCurrency(number, unit, precision)`, `H.numberToPercentage(number, precision)`, `H.numberToHuman(number, precision)`, `H.numberToHumanSize(number, precision)`, `H.numberWithPrecision(number, precision, significant, delimiter, separator, stripInsignificantZeros)`, `H.numberWithDelimiter(number, delimiter, separator)`, `H.numberToDelimited(number, delimiter, separator)`, and `H.numberToPhone(number, areaCode, delimiter, extension, countryCode)` instead of repeated raw `data-*`/`aria-*` strings or hand-written helper ERB. Raw ERB is a narrow escape hatch and must be explicit with `@:railsAllowRawErb`; it is not the default app-authoring destination.

The typed form-builder tag set also includes `<search_field name=${Todo.f.title}>` and `<email_field name=${DeviseFormFields.email}>`, which lower to Rails `form.search_field`/`form.email_field` while preserving checked field refs and helper attrs.

## Design Contract

- Keep one Ruby compiler pipeline. RailsHx is not a second backend.
- Use `ruby_first` as the default Rails authoring contract. Ruby/Rails conventions win when there is a real target-specific choice.
- Keep `portable` useful for domain code and shared Haxe modules. Portable code should still emit idiomatic Ruby where behavior-preserving.
- Do not add a Ruby `metal` profile. Performance and runtime policy should use explicit optimizers or runtime defines only if they gain real tests and docs.
- Keep raw `__ruby__` out of app code. Add typed std/runtime wrappers for Rails APIs that need target-specific lowering.
- Emit Rails-native Ruby: `ApplicationRecord`, `ActiveRecord::Relation`, `ActionController`, `config/routes.rb`, timestamped migrations, Rails route helpers, and Rails tests.

## Implementation Roadmap

Tracked by the `RailsHx typed Rails compiler` epic (`haxe.ruby-wpi`):

- `haxe.ruby-wpi.1` (closed): document the RailsHx architecture contract and agent guidance.
- `haxe.ruby-wpi.2`: implement ActiveRecord schema registry and typed column metadata.
- `haxe.ruby-wpi.3`: implement typed `Relation<T, TCriteria>` and ActiveRecord query DSL. The production query spine exists for typed model/relation `where`/`rewhere` criteria that survive inferred `all`/`distinct`/`none`/`reverseOrder`/`readOnly`/`or`/`merge`/`select`/`order`/`reorder`/`limit`/`offset` relation chains, typed field predicates through fluent field helpers, typed association `includes`/`preload`/`joins`/`eagerLoad` including nested association hashes, typed Rails scope declarations through `@:railsScope` and `@:railsDefaultScope`, typed `find`/`findBy`/`exists`, typed `count`, typed field/expression order tokens including `Order.many([...])`, typed single-field `pluck`, typed named multi-field `Projection.pluck`, grouped aggregate `Projection.group`, typed `Group.count` and `Group.countHaving` for supported map key types, typed aggregate predicates, typed `minimum`/`maximum`, typed `sum`/`average` for `Int` field refs, typed transactions/locking, explicit SQL escape APIs, and `first`/`last`/`toArray`; joined aggregate projections, multi-model projections, named criteria artifacts, and broader grouped-query key support remain future breadth.
- `haxe.ruby-wpi.4`: implement typed associations, validations, enums, and callbacks.
- `haxe.ruby-wpi.5`: implement typed migration DSL and Rails migration generator. The initial create-table artifact lane exists; follow-up operation validation covers known tables, columns, create-table id/id-type/primary-key/comment/temporary options, initial change-table bulk blocks for typed column/default/null changes, column/index rename, column/reference/index add/remove, table-block foreign-key, unique-constraint, and exclusion-constraint add/remove including guarded constraint forms, check-constraint add/remove, and timestamp add/remove, typed reference/join-table id types, typed reference comments/index/foreign-key options including deferred validation and polymorphic FK rejection, typed column/index comments, reversible comment changes, typed timestamp add/remove, typed join table create/drop, checked extension enable/disable, checked schema create/drop/rename, checked enum create/drop/add/type/value rename, checked column bounds, idempotent create/drop tables, idempotent add/remove columns/references, typed reversible column removal, typed reversible bulk column removal, reference foreign-key names, indexes, idempotent indexes, named indexes, named/idempotent index removal, reversible index renames, checked index enable/disable, typed default/null backfill changes, foreign-key columns, named/idempotent/deferrable foreign keys, named/idempotent foreign-key removal, deferred FK/check validation including table-block validation commands, safe named/idempotent check constraints, generic named constraint validation/removal, checked unique constraints including idempotent creation/removal and using-index conversion, checked exclusion constraints including idempotent creation/removal, reversible destructive operations, and explicit `externalTables` for Rails-owned schema. Production snapshot operations now cover `CreateTable` with typed `id`, `idType`, `primaryKey`, composite `primaryKeys`, `comment`, and `temporary`, `ChangeTable` with typed column/default/null change, column/index rename, column/reference/index add/remove bulk blocks, table-block foreign-key, unique-constraint, and exclusion-constraint add/remove including guarded constraint forms, check-constraint add/remove, and timestamp add/remove, `CreateJoinTable` with typed column options, `DropJoinTable`, `CreateSchema`, `DropSchema`, `RenameSchema`, `CreateEnum`, `DropEnum`, `RenameEnum`, `AddEnumValue`, `RenameEnumValue`, `Column` including string/text/numeric/date/time/binary/json types, `Reference` with typed `type`, comments, index options, foreign-key hashes including deferred validation, and polymorphic FK diagnostics, `Index` with typed comments, using methods, index types, guarded concurrent index algorithms through `disableDdlTransaction`, index lock modes, prefix lengths, opclasses, ordered columns, include columns, and nulls-not-distinct options, `EnableExtension`, `DisableExtension`, `DropTableIfExists`, `AddColumnIfNotExists`, `RemoveColumnIfExists`, `RemoveColumnWithType`, `RemoveColumnIfExistsWithType`, `RemoveColumns`, `RemoveColumnsWithType`, `AddReferenceIfNotExists`, `RemoveReferenceIfExists`, `AddCompositeIndex`, `RemoveCompositeIndex`, `RenameIndex`, `EnableIndex`, `DisableIndex`, `RemoveIndexByName`, `AddTimestampsIfNotExists`, `RemoveTimestamps`, `AddForeignKey`, `ValidateForeignKey`, `ValidateForeignKeyByName`, `RemoveForeignKeyByName`, `RemoveForeignKeyByNameIfExists`, `AddUniqueConstraint`, `AddUniqueConstraintIfNotExists`, `AddUniqueConstraintUsingIndex`, `RemoveUniqueConstraint`, `RemoveUniqueConstraintIfExists`, `AddExclusionConstraint`, `AddExclusionConstraintIfNotExists`, `RemoveExclusionConstraint`, `RemoveExclusionConstraintIfExists`, `RenameColumn`, `RenameTable`, `ChangeNull`, `ChangeNullWithDefault`, `ChangeDefault`, `ChangeColumnComment`, `ChangeTableComment`, check constraints including idempotent add/remove and deferred validation, decimal precision/scale, migration version metadata, and explicit SQL/data migrations with rollback. Remaining breadth includes richer change-table subcommands, schema-history inference, and generator integration.
- `haxe.ruby-wpi.6`: implement typed controllers, params, and action results.
- `haxe.ruby-wpi.7`: harden route helper sync and Haxe-first routing. Rails-owned route sync from `rails routes` remains first-class for adoption, while generator-created greenfield apps can use Haxe-owned `@:railsRoutes` sources that emit normal `config/routes.rb`. See [RailsHx Routing Design](railshx-routing-design.md).
- `haxe.ruby-wpi.8`: implement RailsHx generators, rake tasks, and adoption tooling. The initial mixed-app adoption sample exists through `examples/rails_interop_app`; Ruby-native app, route, scaffold, explicit adopt generators, Rails `hxruby:*` generator adapters, suggest-only adoption discovery, source-backed service signature inference, source-backed extension contract generation, an initial deterministic RBS service inference subset, checked `db/schema.rb` model adoption, and `hxruby:doctor`/`hxruby:check` health gates now exist. Richer YARD/schema edge coverage, ownership manifests, and model/migration/controller generator hardening remain follow-up work.
- `haxe.ruby-wpi.9`: add end-to-end Rails integration app and CI gate.
- `haxe.ruby-wpi.10`: write RailsHx guides and API references.
- `haxe.ruby-wpi.11`: plan post-core Rails surfaces such as ActionMailer, ActiveJob, ActionCable, ActiveStorage, ActionView/Hotwire, and ActiveSupport.
- `haxe.ruby-wpi.12`: implement typed Rails ActionView templates inspired by haxe.elixir.codex HXX/HEEx.

The first implementation slice should prove the complete CRUD path: typed model, typed migration, typed params/controller, typed route helpers, generated Rails Ruby, `rails db:migrate`, and `rails test`.

Production readiness is tracked separately by `haxe.ruby-bjv`; see
[RailsHx Production Readiness](railshx-production-readiness.md). The production
epic exists because compiler coverage and fixture breadth are not enough by
themselves: RailsHx must also prove mandatory Rails runtime CI, deployable
dogfood evidence, escape-hatch/security policy, generator UX, gradual adoption,
and a public support/readiness contract.

For the current query authoring contract, see [RailsHx Typed ActiveRecord Query Guide](railshx-query-guide.md). For the expression/Arel boundary, see [RailsHx Query Expression Design](railshx-query-expression-design.md). For the design rationale behind named multi-field projections and grouped counts, see [RailsHx Multi-Field Projection And Grouping Design](railshx-projections-grouping-design.md). The short version: keep chains Rails-shaped, use generated field/association refs for compile-time checks, use `Expr`/`Predicate`/`Aggregate` for typed SQL functions and predicates, use `Projection.pluck(...)`/`Projection.group(...)`/`Group.count(...)`/`Group.countHaving(...)` when result types depend on field refs, and cross from `Relation<T>` to loaded arrays with `toArray()` at controller/template boundaries.

For typed controller and params authoring, see [RailsHx Controller And Params Guide](railshx-controller-guide.md). It covers `@:railsController`, contextual `static final lifecycle = { ... }` declarations for filters/rescues, `ParamsMacro.requirePermit(...)`, nested strong params, flash/session/cookies, request/response facades, status tokens, and typed template rendering.

For Haxe-owned and Rails-owned route source-of-truth modes, see
[RailsHx Routing Design](railshx-routing-design.md). The focused route compiler
fixture is `examples/rails_routes_dsl`, and the full end-to-end route dogfood
is `examples/todoapp_rails`.

Typed Rails templates are part of the RailsHx destination. `haxe.ruby-wpi.12` now has an initial HHX-first ActionView path through Rails HHX plus `H`/`HtmlNode`/`HtmlAttr`, including the todoapp index shell, typed locals-object projection, conditionals, loops, partial composition, `Template.of` owned partial refs, `Template.existing` interop partials, route-helper links/buttons, `link_to`/`button_to` block slots, `Component<TLocals>` capture/render components, typed model field refs in form helpers, and helper-tag static/expression child text. Raw ERB is retained only as an explicit migration/interop escape hatch or Rails-owned external source in gradual-adoption examples. The remaining work is broader component ergonomics, more helper slots, stricter typed assigns/components, richer adopt generators, richer typed `Relation<T, TCriteria>` querying, and continued expansion of HHX coverage while preserving Rails-native output.

For post-core Rails surfaces, see [RailsHx Post-Core Roadmap](railshx-post-core-roadmap.md). ActionMailer now has an initial typed mailer/template slice; see [RailsHx ActionMailer Guide](railshx-action-mailer-guide.md). ActiveJob now has an initial typed job/enqueue slice; see [RailsHx ActiveJob Guide](railshx-active-job-guide.md). ActiveStorage now has initial typed attachment refs; see [RailsHx ActiveStorage Guide](railshx-active-storage-guide.md). Turbo now has initial typed Haxe client helpers and server-side typed stream helpers; see [RailsHx Turbo Guide](railshx-turbo-guide.md). ActionCable now has an initial typed channel/subscription slice; see [RailsHx ActionCable Guide](railshx-action-cable-guide.md). The next typed Hotwire direction is shared backend/frontend contracts for streams, payloads, targets, HHX locals, and browser hooks; see [RailsHx Full-Stack Hotwire Design](railshx-full-stack-hotwire-design.md). ActiveSupport instrumentation now has an initial typed event/payload slice; see [RailsHx ActiveSupport Instrumentation Guide](railshx-instrumentation-guide.md). RailsHx components now have an initial typed ActionView capture/render slice; see [RailsHx Components Guide](railshx-components-guide.md). Rails engine/plugin affordances now have an initial engine-local output-root and generator slice; see [RailsHx Engines And Plugins Guide](railshx-engines-plugins-guide.md). Broader Turbo stream helper coverage and Rails runtime assertions remain post-core follow-up work.

For the current RailsHx DSL ergonomics audit and compatibility policy, see
[RailsHx DSL Ergonomics Audit](railshx-dsl-ergonomics-audit.md).

## Acceptance Bar

RailsHx work is ready when:

- Haxe code type-checks Rails model/query/controller/migration mistakes before Ruby or Rails runtime.
- Generated Ruby is recognizable Rails code a Rails maintainer would accept.
- Framework boundaries are typed in `std/rails/**` or runtime helpers, not scattered as raw app-level injection.
- Fixtures cover successful generated Ruby and negative macro/type failures.
- Rails integration tests compile Haxe, run migrations, boot Rails where available, and execute request/model flows.
- Docs show both Haxe source and generated Rails-shaped Ruby for each major surface.
